// Generated by CoffeeScript 1.3.3
var soma, wings;

soma = require('soma');

wings = require('wings');

soma.chunks({
  Register: {
    meta: function() {
      return new soma.chunks.Base({
        content: this
      });
    },
    prepare: function() {
      return this.template = this.loadTemplate('/build/common/templates/home.html');
    },
    build: function() {
      this.setTitle('The Puzzle School');
      return this.html = wings.renderTemplate(this.template);
    }
  }
});

soma.views({
  Register: {
    selector: '#content .home',
    create: function() {
      $('.register_flag').hide();
      return $('#base').data('base').showRegistration();
    }
  }
});

soma.routes({
  '/register': function() {
    return new soma.chunks.Register;
  }
});
