> auto-backgrounded — the harness completion notification arrives on its own when this exits; you do NOT need to do anything to receive it, and waiting adds nothing. KEEP WORKING: continue with anything that does not depend on this result — the notification interrupts you when it lands. "Do not poll" does NOT mean "stop working": ending the turn to wait is the one response that is never correct. ONLY if you need the result inside the CURRENT turn, block with: codeyam-editor editor wait-for /tmp/claude-0/-workspace/f2576ad4-1439-4975-954e-1c004f0c4467/tasks/b8rdzafzo.output (ONE call, bare — never piped through `| tail`; its budget is capped to fit the tool-call ceiling, so a long run returns WAIT_FOR STILL_RUNNING in-call and the notification still arrives. Re-issue only on LIKELY_WEDGED; do NOT hand-roll an `until grep … sleep` loop, the harness blocks it). Just want to know it is still alive? Read /tmp/claude-0/-workspace/f2576ad4-1439-4975-954e-1c004f0c4467/tasks/b8rdzafzo.heartbeat — one line, always the newest heartbeat; the output file below stays clean of them. Output too long? You do not need `| tail`: run it bare and read the `CODEYAM_FULL_OUTPUT` line the command prints — it names a file holding the COMPLETE output, which you can `cat` afterwards.
{"command":"pre-commit-sync","elapsed_secs":0,"emitted_at_unix":1786972279,"heartbeat":"CODEYAM_CMD_RUNNING","note":"still running","pid":622893}
Commit queue: already holding head (uuid=9ceb007c-8094-412a-bba0-bac7b3d6fba4, branch=main). Reusing it — no new queue entry. Retained until a successful push or an explicit release. (See docs/commit-queue.md.)
Commit queue: head acquired (uuid=9ceb007c-8094-412a-bba0-bac7b3d6fba4, branch=main, remote=origin). Tail will run under serialized order. (See docs/commit-queue.md.)
{"commitsPulled":0,"fastPathReason":"ls-remote matched local @{upstream}","headAcquired":false,"pulled":false,"synced":true}
