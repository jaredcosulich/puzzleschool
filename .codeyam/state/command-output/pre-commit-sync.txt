> auto-backgrounded — the harness completion notification arrives on its own when this exits; you do NOT need to do anything to receive it, and waiting adds nothing. KEEP WORKING: continue with anything that does not depend on this result — the notification interrupts you when it lands. "Do not poll" does NOT mean "stop working": ending the turn to wait is the one response that is never correct. ONLY if you need the result inside the CURRENT turn, block with: codeyam-editor editor wait-for /tmp/claude-0/-workspace/291dbe83-4440-4241-b50d-050ddc68e834/tasks/bhxvqbunc.output (ONE call, bare — never piped through `| tail`; its budget is capped to fit the tool-call ceiling, so a long run returns WAIT_FOR STILL_RUNNING in-call and the notification still arrives. Re-issue only on LIKELY_WEDGED; do NOT hand-roll an `until grep … sleep` loop, the harness blocks it). Just want to know it is still alive? Read /tmp/claude-0/-workspace/291dbe83-4440-4241-b50d-050ddc68e834/tasks/bhxvqbunc.heartbeat — one line, always the newest heartbeat; the output file below stays clean of them. Output too long? You do not need `| tail`: run it bare and read the `CODEYAM_FULL_OUTPUT` line the command prints — it names a file holding the COMPLETE output, which you can `cat` afterwards.
{"command":"pre-commit-sync","elapsed_secs":0,"emitted_at_unix":1786743163,"heartbeat":"CODEYAM_CMD_RUNNING","note":"still running","pid":324408}
Commit queue: enqueuing on branch 'puzzle-school-rebuild'... (normal; serializes pushes when multiple sessions commit concurrently. See docs/commit-queue.md.)
Commit queue: enqueued (uuid=b59dd592-0648-4be0-a788-3facbc7de323). Waiting for head. If you're the only session this clears in ~1s; otherwise inspect with `codeyam-editor editor branch-queue status`.
Commit queue: now BLOCKING until this session reaches the head of the queue on 'puzzle-school-rebuild'. This is EXPECTED, not a hang — it may take a while on a deep queue. You will be returned automatically the moment head is acquired — that automatic return IS your completion notification, so there is nothing to poll for. Periodic `Commit queue: still waiting — position N of M …` lines are normal progress. Run this ONCE and let it block: do NOT poll `branch-queue status` in a loop, do NOT re-read queue position on a timer, do NOT re-run the command, and do NOT ask the operator whether to wait or skip. The operator can interrupt the command to force a skip. (See docs/commit-queue.md.)
✅ Commit queue: HEAD ACQUIRED after 0s — proceeding with sync. This return IS the head-acquired signal; do not poll or re-run.
Commit queue: head acquired (uuid=b59dd592-0648-4be0-a788-3facbc7de323). Proceeding now — the blocking wait is over; the tail will run under serialized order.
Commit queue: head acquired (uuid=b59dd592-0648-4be0-a788-3facbc7de323, branch=puzzle-school-rebuild, remote=origin). Tail will run under serialized order. (See docs/commit-queue.md.)
{"commitsPulled":0,"fastPathReason":"ls-remote matched local @{upstream}","headAcquired":true,"pulled":false,"synced":true,"waitedSec":0}
