> auto-backgrounded — the harness completion notification arrives on its own when this exits; you do NOT need to do anything to receive it, and waiting adds nothing. KEEP WORKING: continue with anything that does not depend on this result — the notification interrupts you when it lands. "Do not poll" does NOT mean "stop working": ending the turn to wait is the one response that is never correct. ONLY if you need the result inside the CURRENT turn, block with: codeyam-editor editor wait-for /tmp/claude-0/-workspace/b2024ceb-c46f-4c1c-b529-7714ec71c7ce/tasks/bbotekzwb.output (ONE call, bare — never piped through `| tail`; its budget is capped to fit the tool-call ceiling, so a long run returns WAIT_FOR STILL_RUNNING in-call and the notification still arrives. Re-issue only on LIKELY_WEDGED; do NOT hand-roll an `until grep … sleep` loop, the harness blocks it). Just want to know it is still alive? Read /tmp/claude-0/-workspace/b2024ceb-c46f-4c1c-b529-7714ec71c7ce/tasks/bbotekzwb.heartbeat — one line, always the newest heartbeat; the output file below stays clean of them. Output too long? You do not need `| tail`: run it bare and read the `CODEYAM_FULL_OUTPUT` line the command prints — it names a file holding the COMPLETE output, which you can `cat` afterwards.
{"command":"diagnose-preview","elapsed_secs":0,"emitted_at_unix":1786993703,"heartbeat":"CODEYAM_CMD_RUNNING","note":"still running","pid":918247}
━━━ Preview Diagnostic: / ━━━

  proxy port 4321 — ERROR: error sending request for url (http://127.0.0.1:4321/)
  direct port 4322 — HTTP 200, Transfer-Encoding: chunked, no Content-Length

Most likely cause: proxy is not reachable on port 4321, but the app IS serving directly on port 4322 — a proxy-only wedge. `restart-dev-server` will NOT help (its health check routes through the wedged proxy); restart the editor server instead: `codeyam-editor editor restart-server`. In this cloud container, restart with `--force-in-container`: `codeyam-editor editor restart-server --force-in-container`.
